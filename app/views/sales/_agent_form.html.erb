<%= simple_form_for(@sale) do |f| %>
  <%= f.input :verified_by_agent, label: false, as: :hidden, input_html: {value: true} %>
  <%= f.input :status, label: false, as: :hidden, input_html: {value: 1} %>
  <%= f.button :submit, 'Buyurtmani yakunlash', class: 'btn-success' %>
<% end %>
